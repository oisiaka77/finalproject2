desc "Hydrate the database with some sample data to look at so that developing is easier"
task({ :sample_data => :environment}) do



  # first_names = ["Pat", "Raghu", "Jelani"]
  # last_names = ["Sun", "Moon", "Star"]
  
  #  3.times do |count|
  #     user = User.new
  #     user.username = first_names.at(count) + rand(10).to_s
  #     user.email = first_names.at(count) + "@gmail.com"
  #     user.first_name = first_names.at(count)
  #     user.last_name = last_names.at(count)
  #     user.save
   end

end
